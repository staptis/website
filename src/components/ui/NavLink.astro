---
import { cn } from "@/utils/cn";

interface Props {
  href: string;
  label: string;
}

const { href, label } = Astro.props;
const currentPath = Astro.url.pathname;
const isActive = currentPath === href;
const className = cn(
  "hover:underline-primary hover:text-primary underline-offset-4 hover:underline",
  isActive && "text-primary underline-primary underline",
);
---

<a href={href} class={className} aria-current={isActive ? "page" : undefined}
  >{label}</a
>
